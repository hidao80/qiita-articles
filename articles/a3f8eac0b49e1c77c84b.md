---
title: VS Codeでgitのリポジトリが表示されない
tags: VSCode Git リポジトリ
author: hidao
slide: false
---
## TL;DR

`Ctlr+Shift+U`（macは`Shift+Cmd+U`）を押して、出力ペインから`Git`を選択して表示されている`safe.directory`設定を行うんだ…！

```sh
git config --global --add safe.directory c:/git_repos/repo_dir
```

![Visual_Studio_Code-2.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/76242/ddb68004-dd2e-11e7-405f-d8316393a785.png)


## 想定している状態

PCをクリーンインストールまたは新しく買ってきた場合、ホームディレクトリ内に`.gitconfig`ファイルがなく、VS CodeのGit自動実行時にカレントディレクトリがセーフディレクトリ指定されていないことがあります。
VS Codeで`.git`ディレクトリが含まれるディレクトリを開いてもリポジトリがあると認識してもらえません。

このとき、VS Codeの出力ペインの`Git`の出力を見ると、次のような出力が確認できます。

```sh
[2022-09-07T22:49:11.156Z] fatal: detected dubious ownership in repository at 'C:/git_repos/repo_dir'
To add an exception for this directory, call:

	git config --global --add safe.directory C:/git_repos/repo_dir
```

## 対処法

素直に出力に表示されているコマンドを実行します。その後、VS Codeを閉じて起動し直すとセーフディレクトリ設定が反映され、リポジトリとして認識されます。

```sh
git config --global --add safe.directory C:/git_repos/repo_dir
```

## 設定の確認

ホームディレクトリにある`.gitconfig`に、セーフディレクトリ登録したリストが表示されているのでそれを目視できます。

```config:~/.gitconfig
[user]
	name = your_user_name  # git config --global user.name で表示されるもの　
	email = 12345678+your_user_name@users.noreply.github.com　　# git config --global user.email で表示されるもの
[safe]
	directory = C:/git_repos/repo_dir     # さっきのコマンドで登録したもの
    directory = ...                       # 登録した数だけ列挙される
```

ちなみに、コマンドを実行しなくてもこの`~/.gitconfig`の`[safe]`以下をエディタで編集してもセーフディレクトリ設定は有効になります。

## 参考リンク

- [git config --global --add safe.directory '...' が出たとき - Qiita](https://qiita.com/RoaaaA/items/2ca93e2ea585b321f777)
